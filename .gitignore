# Python
__pycache__/
*.py[cod]

# Never commit the Travelpayouts token — it lives in GitHub repo secrets
.env
*.token
secrets.*

# Local tooling config (machine-specific absolute paths)
.claude/

# OneDrive / editor cruft
Thumbs.db
desktop.ini
.vscode/
